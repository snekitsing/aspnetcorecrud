@page
@model ASPNET_CORE_CRUD_APP.Pages.ClientsModel
@{
}
<br>
<h2>Clients</h2>
<a class="btn btn-primary" href="/Create" role="button">Add New</a>

<hr />

<br>
<table class="table">
    <thead>
        <tr>
            <td>ClientID</td>
            <td>Name</td>
            <td>Email</td>
            <td>Phone</td>
            <td>Options</td>
        </tr>
    </thead>
    <tbody>
        @foreach (var client in Model.Client)
        {
            <tr>
                <td>@client.ClientID</td>
                <td>@client.Name</td>
                <td>@client.Email</td>
                <td>@client.Phone</td>
                <td>
                    <a type="button" class="btn btn-primary" href="EditClient?id=@client.ClientID">Edit</a>
                    <a type="button" class="btn btn-danger" href="/DeleteClient?id=@client.ClientID">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>